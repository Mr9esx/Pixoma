## 1. MCP 失败文案

- [x] 1.1 将 `/mcp`、`/sse` 的 401/403 正文改为指导 AI 检查连接器、不要索要 token 的句子，并改现有鉴权测试
- [x] 1.2 将 tool 错误（权限不允许、工作流不存在/停用、缺必填、任务不可见）改为带下一步的文案，并改 `tools_test`

## 2. Skill 包

- [x] 2.1 写 `docs/skills/pixoma-mcp/SKILL.md`：前置（聊天需 MCP 连接器）、调用顺序、错误对照表（无工具 / 401/403 / 权限 / 缺字段 / 停用 / 任务不可见）
- [x] 2.2 核对 description 只写触发条件；包不在 `.agents/skills`

## 3. 安装提示词

- [x] 3.1 文档写一句不含密钥的安装提示词（公网 URL + 需 MCP 连接器）
- [x] 3.2 注明仓库相对路径

## 4. 验收

- [x] 4.1 对照 spec：提示词、调用链从略鉴权、无工具提醒、接口指导文案、skill 错误表、不进开发技能树
