## 1. 图数据

- [x] 1.1 先写 `buildLinkGraph` 测试：四层连线、多入口合并为一条边、全量丢掉度数 0、焦点图保留自身、健康态来自 `*References`
- [x] 1.2 实现 `buildLinkGraph` + 路径高亮集合；不复制断点规则
- [x] 1.3 测试覆盖「缺入口但仍有下游则出现在全量图」

## 2. 只读画布

- [x] 2.1 自定义节点（类型名、资源名、`StatusDot`）、稳定 `nodeTypes`、引入 xyflow 样式
- [x] 2.2 `LinkGraph`：四列坐标、固定高度容器、`fitView`/`Controls`、不可拖改配置、点选高亮路径、「打开详情」
- [x] 2.3 合同测试：只读、高亮不跳转、打开详情才有路由、健康未就绪不标绿

## 3. 工作台卡片

- [x] 3.1 `WorkbenchDataBoard` 顶部增加配置拓扑 `Card`，独立查询与失败隔离
- [x] 3.2 合同测试：工作台含拓扑卡片；拓扑失败不影响其他区块

## 4. 详情弹窗

- [x] 4.1 `TopologyDialog` + 四页「拓扑」按钮（工作流 / 消息平台 / 任务队列 / 计算节点）
- [x] 4.2 合同测试：四页入口存在；不移除链路健康区块；焦点图不含无关工作流

## 5. 文案与验证

- [x] 5.1 i18n：卡片标题「配置拓扑」、按钮「拓扑」「打开详情」；无解释性说明句
- [x] 5.2 `pnpm tsc -b` 与相关 vitest；浏览器走通工作台全图与任一详情弹窗点选/打开详情
