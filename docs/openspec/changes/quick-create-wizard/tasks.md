## 1. 会话与文案

- [x] 1.1 写失败测试：旧 schema 会话加载后被清空；新草稿含 topicKey / selectedEdgeId
- [x] 1.2 实现 session schemaVersion=3 与清空逻辑
- [x] 1.3 侧栏与 i18n 改为「快速新建」（zh/en），路由仍为 `/quick-config`

## 2. 落地页

- [x] 2.1 写失败测试：落地页无已有工作流表；无草稿时只有新建入口
- [x] 2.2 落地页去掉工作流表与「选已有」下一步；保留继续上次（有草稿时）

## 3. 队列与节点步骤

- [x] 3.1 写失败测试：未选队列不能进节点步；默认队列被记录为 `default`
- [x] 3.2 实现队列步：已有队列列表（默认置顶）+ 新建 Topic；只收集草稿
- [x] 3.3 写失败测试：队上无人时未选节点不能前进；队上有人时不选也能前进
- [x] 3.4 实现节点步：列表 + CreateEdgeWizard；不写扩容文案；跳过通线仍可选中该节点
- [x] 3.5 调整 Formity 为四屏：工作流 → 队列 → 节点 → 还差一步；去掉投放步与完成页门禁
- [x] 3.6 向导内 WorkflowEditor 增加隐藏处理流程的开关，第一步不渲染 TaskFlowTable

## 4. 提交启用

- [x] 4.1 写失败测试：选默认队列提交后 Case 启用且有一条无条件规则指向 `default`
- [x] 4.2 写失败测试：选非默认队列提交后 Case 有一条无条件规则指向该 topic
- [x] 4.3 写失败测试：选了未订阅节点则 patch subscribe_topics 追加；跳过节点则不 patch
- [x] 4.4 实现节点步下一步的提交链：createTopic（若新）→ createCase → 按需 patchEdge → enableCase；失败留在节点步
- [x] 4.5 启用成功后禁止回跳到第 1–3 步改草稿

## 5. 还差一步

- [x] 5.1 写失败测试：第四屏有进入消息平台的按钮；无投放不阻止到达本屏
- [x] 5.2 写失败测试：选中节点未通或队上全离线时展示「节点还没通」
- [x] 5.3 实现第四屏标题「还差一步」、短教程与按钮跳转 `/channels`

## 6. 收尾

- [x] 6.1 更新 `quick-config.contract.test.ts` / readiness 测试，删除投放门禁与处理流程清单断言
- [x] 6.2 对照 specs 场景跑相关 vitest，确认空白路径：工作流 → 默认队列 → 新建节点（可不通）→ 已启用 → 还差一步
