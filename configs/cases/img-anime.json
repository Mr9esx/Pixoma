{
  "id": 1,
  "name": "二次元文生图",
  "description": "输入一句话，生成二次元风格插画（mock）。",
  "preview": "示例：少女站在樱花树下，柔光",
  "price": 10,
  "tags": [
    "image",
    "text2img",
    "anime"
  ],
  "menu_key": "image",
  "categories": [
    "generate"
  ],
  "inputs": [
    {
      "key": "prompt",
      "type": "string",
      "required": true,
      "description": "画面描述（中英文均可）"
    },
    {
      "key": "seed",
      "type": "number",
      "required": false,
      "skip_allowed": true,
      "description": "可选随机种子，可跳过"
    }
  ],
  "outputs": [
    {
      "key": "image",
      "type": "image"
    }
  ],
  "bindings": {
    "workflow": {
      "1": {
        "class_type": "Stub",
        "inputs": {}
      }
    },
    "inputs": [
      {
        "key": "prompt",
        "node_id": "1",
        "field_path": "text"
      }
    ],
    "outputs": [
      {
        "key": "image",
        "node_id": "1",
        "index": 0
      }
    ]
  },
  "input_schema": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "prompt"
    ],
    "properties": {
      "prompt": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000
      },
      "seed": {
        "type": "integer",
        "minimum": 0
      }
    }
  }
}