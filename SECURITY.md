# 安全策略

## 支持版本

Pixoma 处于公开发布的早期阶段。请始终使用最新稳定版本；安全问题修复只会应用于最新主分支和其后的最新发布。

## 报告漏洞

请不要在公开 Issue、Pull Request、讨论或社交媒体中披露未修复的安全漏洞。

请通过 [GitHub Security Advisories](https://github.com/Mr9esx/Pixoma/security/advisories/new) 私下报告。请尽量包含：

- 受影响的组件、接口、版本或提交；
- 复现步骤和最小 PoC；
- 影响评估与可能的修复建议；
- 是否包含个人数据、凭证、任务图片或其他敏感数据。

我们会在 3 个工作日内确认收到；确认后 14 个工作日内给出初步评估。修复协调期间请保持信息私密。

## 部署要求

- 不要将数据库 DSN、对象存储密钥、Telegram Bot Token、Agent Token 或数据目录提交到版本库。
- 控制面默认只监听 `127.0.0.1:8080`。公网或跨主机访问必须置于 TLS 反向代理之后，并配置访问控制。
- 生产部署必须设置强唯一管理员密码，并要求所有 Edge Agent 使用各自独立 Token。
- 建议通过 `PIXOMA_ENCRYPTION_KEY` 提供长且随机、受控保存的加密密钥；不要提交到仓库。
- 本地数据目录包含引导配置、数据库和 Blob 文件；控制面会创建 `0700` 目录和 `0600` 文件，请避免放在共享目录或可被其他账号读取的位置。
- 长期运行请开启依赖审计与漏洞扫描，并定期升级。
