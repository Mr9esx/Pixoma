---
description: 架构变更时必须同步更新 docs/architecture；纯局部小改不动文档
alwaysApply: true
---

# 架构文档同步

任何需求/改动在**影响整体架构设计**时，必须同步更新 `docs/architecture/`（至少改动到的专章 + 索引若有新增文件）。纯局部、不影响架构边界与主链路的小修改不必改文档。

## 需要同步（必要）

触及以下任一，视为架构相关，交付前更新对应文档：

| 变化 | 主要文档 |
|---|---|
| 限界上下文 / 包边界 / 依赖方向 | `bounded-contexts.md`、`overview.md` |
| 主链路、事件 topic、调度/执行角色、状态机 | `runtime.md`、`overview.md` |
| 表/字段/ER、持久化真相源、实例关系 | `data-model.md` |
| 系统拓扑、外部系统、进程组装形态 | `overview.md`、`diagrams/system.html` |
| 新增架构专章 | `docs/architecture/README.md` |

并视需要更新根 `README.md` 中的架构入口链接。

## 不必同步（不必要）

- 单文件 bugfix、重命名、注释、测试补强
- 既有接口内的局部逻辑，不改边界、表、事件、外部依赖
- 文案/样式/配置默认值微调且不改变运行时拓扑

## 自检

实现收尾前问一句：本次是否改变了「模块怎么切、数据怎么存、请求/事件怎么走、和外部系统怎么连」？是 → 改文档；否 → 跳过。
