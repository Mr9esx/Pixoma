---
description: ComfyUI mock 必须可开关，并与真实功能同步可跑通
alwaysApply: true
---

# Comfy Mock 同步演进

本项目以 **可配置 mock** 支撑本地/联调；真实 ComfyUI 通过同一 `comfyui.Client` 切换。

## 硬性要求

1. **一键开关**：`configs/bot.yaml` 的 `comfy_mock`（或 `COMFY_MOCK`）控制 Mock / 真实 HTTP；不要写死只走 Mock。
2. **功能与 Mock 同改**：任何影响执行链路的需求（协议、Case、产物类型、状态事件、TG 投递等），必须同步更新 Mock，保证 `comfy_mock: true` 时端到端仍能成功完成主路径。
3. **选型入口**：新增/替换 Comfy 客户端用 `comfyui.NewClient`；测试可直接构造 `*comfyui.Mock`。
4. **禁止**：只改真实 HTTP 路径而让 Mock 过期；或移除开关导致无法本地无 Comfy 冒烟。

## 验收自检

- Mock 开：确认生成 → 收到产物（至少图片链路）
- Mock 关：能连上 `comfyui_base_url` 时走真实 Submit/Wait
