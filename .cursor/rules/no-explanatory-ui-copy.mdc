---
description: 产品界面文案只写控件本身，不补解释性废话
alwaysApply: true
---

# 产品文案

界面只保留：字段名、选项名、按钮、校验/错误。

不要写「为什么没有这项」「请去某页」「这里不检查 / 不配置」这类说明。删掉一项之后，不要补一句交代它去哪了。

```tsx
// 错
<p>Comfy 节点在「实例」里增删，这里不配地址。</p>
<Link to="/instances">去实例页</Link>

// 对：不配就不出现。节点页在侧栏里。
```
